<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- profile 包也允许明文 HTTP，便于性能分析时连本地后端 -->
    <application android:usesCleartextTraffic="true"/>
</manifest>
