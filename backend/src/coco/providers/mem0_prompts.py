"""Mem0 中文抽取约束：拼进 custom_instructions，不改 mem0 系统 JSON 协议。"""

from __future__ import annotations

# mem0 2.x 用 ADDITIVE_EXTRACTION_PROMPT 要求返回 {"memory":[{"text":...}]}；
# 这里只补充业务边界，避免抽寒暄 / 一次性信息 / 医疗结论。
COCO_MEMORY_CUSTOM_INSTRUCTIONS = """
你在为一位中国老人的家庭陪伴助手整理长期记忆。请严格遵守：

1. 只提取稳定、长期仍有用的事实，写成简短中文陈述句（如「喜欢吃红烧肉」「平时自己做饭」）。
2. 必须保留：饮食喜好与忌口、做饭习惯、作息、兴趣爱好、家人称呼与关系、常去的地方、个人基本情况。
3. 不要提取：寒暄、一次性闲聊、天气、临时安排、当通情绪波动、重复确认语。
4. 不要提取：药盒文字、账单金额、验证码、一次性通知、医疗诊断或药量建议。
5. 助手说的话只有在「用户明确接受了建议」时才可记；不要把助手的猜测当成用户事实。
6. 每条 memory.text 用「用户……」或省略主语的陈述，不要写成对话原文。
""".strip()
