# Coco 架构约定

> 目标：结构简单、边界清晰、后续功能可直接往里加。  
> `back/` 只读参考，新代码不要照搬。

## 1. 仓库目录

```text
coco/
  backend/          FastAPI 后端
  frontend/         Flutter 客户端（iOS + Android + Web；音频/通知走平台适配）
  Dockerfile        Web + API 一体镜像（库连外部 Postgres；docker build/run）
  docker.env.example  容器环境变量模板
  doc/              产品与设计文档
  AGENTS.md         AI / 协作者约束
  README.md         启动说明
  back/             旧实现（只读，勿改）
```
## 2. 技术栈

| 层 | 选型 |
|---|---|
| 客户端 | Flutter / Dart（iOS + Android + Web），Riverpod，go_router，Dio；采麦/PCM 播/本地通知按平台分流 |
| 后端 | FastAPI，SQLAlchemy 2 async，Alembic，uv；长期记忆用 Mem0 OSS + pgvector（同库） |
| 数据库 | PostgreSQL，独立 schema `coco`（需 `vector` 扩展供 Mem0） |
| 鉴权 | 手机号验证码；JWT access + opaque refresh |
| AI / 语音 | 阿里云百炼（Realtime / 文本 / 识图 / 文生图 / Embedding）；记忆经服务端 Mem0 适配层 |

## 3. 后端分层

只有两层：

```text
modules/<feature>/
  router.py     HTTP、校验、依赖注入
  service.py    业务逻辑，直接用 AsyncSession
  schemas.py    请求 / 响应 DTO
```

约束：

- router 不写 SQL，不直接操作 ORM。
- service 不依赖 FastAPI Request / Response。
- 不做 domain / application / api 三层来回转 DTO。
- 不做全局手写 DI 容器；需要时用 FastAPI `Depends`。

公共横切放在 `src/coco/`：

```text
main.py config.py database.py logging.py errors.py security.py deps.py
models/          ORM 模型
modules/         按功能拆分的路由与服务
```

## 4. 前端分层

按 feature 组织，页面直接 watch 对应 controller：

```text
lib/
  app/                 MaterialApp、router
  core/                theme、network、storage、通用组件
  features/<name>/
    data/              API、Repository 实现
    domain/            模型
    application/       Riverpod Notifier
    presentation/      页面与局部组件
```

约束：

- 不要上帝 Repository（一个类实现全部接口）。
- 不要 AppView / AppActions 双层门面。
- 路由尽量少；登录多步骤用页面内 step，不拆成多条路由。
- 页面不写裸色值，统一走 design token。
- 采麦 / PCM 播 / 本地通知 / 截屏取图走 `core` 与 feature 内条件导入（IO vs Web），业务控制器不直接依赖 `dart:io` 或原生插件。

## 5. 数据库：schema `coco`

所有新表落在 `coco` schema，与旧 `public` 表隔离。

### 5.1 本期表

| 表 | 用途 |
|---|---|
| `users` | 用户；`phone_hash` 查重/OTP，`phone_e164` 明文供绑定子女拨打；角色 `parent` / `child` |
| `phone_codes` | 验证码挑战；存 code_hash，消费后作废 |
| `auth_sessions` | refresh session；只存 token hash，刷新时轮换 |
| `families` / `family_invites` | 家庭绑定与邀请链接令牌（`code` 写入 `/i/{code}` 短链，永不过期直至绑定） |
| `reminders` / `reminder_occurrences` | 日常提醒与触发确认 |
| `care_shares` | 父母授权分享给子女的摘要 |
| `daily_note_settings` | 每日小记：是否自动生成、是否发给子女、参考照 BOS key |
| `daily_notes` / `daily_note_images` | 可可视角日记（title/header/body/closing + extraction）与 BOS 配图 |
| `family_messages` | 子女报平安 / 关怀留言 |
| `notifications` | 站内通知 |
| `conversations` | 父母端语音会话（一次实时通话一条） |
| `conversation_items` | 会话内最终转写与工具调用（父母私有，不存音频） |
| `llm_traces` | 运营调试：每次大模型调用的输入/输出/耗时（不含原图与 PCM） |
| `memories` | 用户主动要求记住的显式记忆（分类 + 来源）；记忆页只读此表 |

Mem0 仍在同库 pgvector collection（默认 `coco_memories`）托管隐式抽取；通话结束后异步抽取。建连注入：显式表优先，Mem0 补足。通话中 `save_memory` 写显式表，`recall_memory` 检索显式 + Mem0。

### 5.2 对话历史约定

- 语音桥接在 `user.final` / `assistant.final` 与工具执行后落库。
- 通话结束时用服务端文本模型根据转写摘录生成短标题写入 `conversations.title`；无 Key 或失败时用预览文案兜底。
- 仅父母本人可读；无子女同步 API；不存原始 PCM。
- 运营后台可通过 `llm_traces` 按用户查看完整模型调用链（含标题、转译、识图、文生图、Mem0）；与 `conversation_items` 分离，避免污染产品历史。
- 字段与枚举以 `doc/需求.md` 为准。

## 6. 鉴权约定

- 手机号规范化为 `+86xxxxxxxxxxx`。
- 用户表同时存 `phone_hash`（HMAC）与 `phone_e164`（明文）；验证码仍只存 HMAC。
- 家庭接口仅向子女返回 `parent_phone`（11 位国内号），用于一键拨打。
- access token：JWT HS256，短过期。
- refresh token：随机串，只返回一次，DB 存 hash；刷新时轮换。
- 错误响应：`{"error":{"code":"...","message":"中文可展示文案"}}`。
- 邀请短链 `GET /i/{code}` 302 到 `/index.html#/invite/{code}`；免鉴权预览 `GET /v1/family/invites/{code}` 按 IP 限流。

## 7. 角色

| 值 | 含义 |
|---|---|
| `parent` | 父母 / 老年用户 |
| `child` | 成年子女 |

JWT `role` 与 `users.role` 一致，表示当前 UI 角色；家庭权限后续由 membership 表单独判断。

## 8. 开发原则

1. 先做最小可运行闭环，再加功能。
2. 关键业务规则、权限边界写中文注释。
3. 不在仓库、日志、测试里写入真实手机号、验证码、密钥。
4. staging / production 禁止开发固定验证码与 fake 短信。
